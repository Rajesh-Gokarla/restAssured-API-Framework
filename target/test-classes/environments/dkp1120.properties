# BASE URLS (MOCKED FOR TESTING)

USER_SERVICE=http://localhost:8080
ACCOUNT_SERVICE=http://localhost:8080
AUTH_SERVICE=http://localhost:8080

AUTH_TYPE=BEARER
TOKEN_SOURCE=API

AUTH_USERNAME=kminchelle
AUTH_PASSWORD=0lelplR
AUTH_ENDPOINT=/auth/login

# Token field in response JSON
TOKEN_JSON_PATH=token

# BUSINESS ENDPOINTS (MOCKED APIs)

# CREATE USER (acts like CREATE_CIF)
CREATE_CIF=/api/cif

# UPDATE USER (acts like UPDATE_KYC)
UPDATE_KYC=/users/{userId}

# CREATE / OPEN ACCOUNT (acts like OPEN_ACCOUNT)
OPEN_ACCOUNT=/carts/add

# LINK USER TO ACCOUNT (simulation)
LINK_CIF_ACCOUNT=/carts/{cartId}

# ACTIVATE CUSTOMER (status update simulation)
ACTIVATE_CUSTOMER=/users/{userId}